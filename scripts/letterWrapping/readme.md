Function that takes an element and splits the text by letters and adds classnames to each div
    Parameters:
        element: html element. eg document.getElementById('myElement');

        returns array of divs